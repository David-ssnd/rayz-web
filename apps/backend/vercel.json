{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=backend",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --ignore-scripts=false",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
