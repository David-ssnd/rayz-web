# --- Database ---
DATABASE_URL=

# --- Authentication (Auth.js) ---
AUTH_SECRET=
AUTH_URL=

# OAuth Providers
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# --- Backend Integration ---
ADAPTER_SECRET=

# --- Realtime Communication (Local / Cloud) ---
# Mode: 'local' (WebSocket bridge) or 'cloud' (Ably)
NEXT_PUBLIC_MODE=
# Local WebSocket bridge URL (when NEXT_PUBLIC_MODE=local)
NEXT_PUBLIC_LOCAL_WS_URL=
# Ably public API key for browser (WebSocket transport, dev only)
NEXT_PUBLIC_ABLY_API_KEY=
# Ably token endpoint URL for browsers (production recommended)
NEXT_PUBLIC_ABLY_TOKEN_URL=
# Ably API key for server-side token generation (keep server-only in production)
ABLY_API_KEY=
